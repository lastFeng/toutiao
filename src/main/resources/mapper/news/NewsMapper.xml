<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.toutiao.mapper.news.NewsMapper">

    <sql id="BaseName">
        id, title, link, image, like_count, comment_count, create_date, user_id
    </sql>

    <sql id="BaseTableName">
        news.id, news.title, news.link, news.image, news.like_count, news.comment_count, news.create_date, news.user_id
    </sql>
</mapper>